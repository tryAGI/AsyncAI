# AsyncAI.CLI

Command-line interface for the AsyncAI SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global AsyncAI.CLI --prerelease
```

## Usage

```bash
async-ai --help
async-ai api --help
```