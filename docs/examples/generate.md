# Generate

Basic example showing how to create a client and make a request.

This example assumes `using AsyncAI;` is in scope and `apiKey` contains your AsyncAI API key.

```csharp
using var client = new AsyncAIClient(apiKey);
```